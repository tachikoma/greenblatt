name: Live Trading Real (Self-Hosted)

# 사용법:
# - 이 워크플로는 `dry_run` 입력을 받습니다. `dry_run=true`이면
#   `run_live_trading.py`에 `--dry-run`을 전달하여 실주문 없이 시뮬레이션을
#   실행합니다(신호·주문의도 계산만 수행).
# - 안전을 위해 워크플로 환경에서 `KIWOOM_MODE`는 `real`로 고정되어 있으니,
#   self-hosted 러너에서 테스트할 때는 `dry_run=true` 사용을 권장합니다.

on:
  # schedule:
    # KST 08:01 (UTC 23:01), Sunday-Thursday
    # - cron: "1 23 * * 0-4"
  workflow_dispatch:
    inputs:
      signal_date:
        description: "신호 기준일 (YYYY-MM-DD, 비우면 실행일)"
        required: false
        type: string
      force:
        description: "이미 실행한 주기도 강제 실행"
        required: false
        default: false
        type: boolean
      dry_run:
        description: "실주문 없이 시뮬레이션 실행 (true/false)"
        required: false
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: live-trading-real-selfhosted
  cancel-in-progress: false

jobs:
  run-live-trading-real:
    runs-on:
      - self-hosted
      - linux
      - arm64
      - kiwoom-real
    timeout-minutes: 45
    env:
      KIWOOM_MODE: real
      KIWOOM_APPKEY: ${{ secrets.KIWOOM_APPKEY }}
      KIWOOM_SECRETKEY: ${{ secrets.KIWOOM_SECRETKEY }}
      KIWOOM_ACCOUNT_NO: ${{ secrets.KIWOOM_ACCOUNT_NO }}
      PYKRX_LOGIN_ID: ${{ secrets.PYKRX_LOGIN_ID }}
      PYKRX_LOGIN_PW: ${{ secrets.PYKRX_LOGIN_PW }}

      DEBUG_SIGNAL_ENABLED: ${{ vars.DEBUG_SIGNAL_ENABLED || 'true' }}
      FUNDAMENTAL_SOURCE: ${{ vars.FUNDAMENTAL_SOURCE || 'pykrx' }}
      COMMON_REQUEST_RETRIES: ${{ vars.COMMON_REQUEST_RETRIES || '3' }}
      COMMON_REQUEST_RETRY_BACKOFF_SECONDS: ${{ vars.COMMON_REQUEST_RETRY_BACKOFF_SECONDS || '0.5' }}
      PYKRX_REQUEST_TIMEOUT: ${{ vars.PYKRX_REQUEST_TIMEOUT || '6.0' }}
      PYKRX_SESSION_DEBUG: ${{ vars.PYKRX_SESSION_DEBUG || '0' }}
      KIWOOM_PREFILTER_ENABLED: ${{ vars.KIWOOM_PREFILTER_ENABLED || 'true' }}
      KIWOOM_PREFILTER_TARGET: ${{ vars.KIWOOM_PREFILTER_TARGET || '500' }}
      KIWOOM_PREFILTER_MIN_MCAP: ${{ vars.KIWOOM_PREFILTER_MIN_MCAP || '50000000000' }}

      INVESTMENT_RATIO: ${{ vars.INVESTMENT_RATIO || '0.95' }}
      NUM_STOCKS: ${{ vars.NUM_STOCKS || '40' }}
      REBALANCE_DAYS: ${{ vars.REBALANCE_DAYS || '' }}
      REBALANCE_MONTHS: ${{ vars.REBALANCE_MONTHS || '3' }}
      ORDER_TIMEOUT_MINUTES: ${{ vars.ORDER_TIMEOUT_MINUTES || '3' }}
      ORDER_PRICE_OFFSET_BPS: ${{ vars.ORDER_PRICE_OFFSET_BPS || '10' }}
      USE_HOGA_RETRY_PRICE: ${{ vars.USE_HOGA_RETRY_PRICE || 'true' }}
      RETRY_PRICE_OFFSET_BPS: ${{ vars.RETRY_PRICE_OFFSET_BPS || '25' }}
      MAX_RETRY_ROUNDS: ${{ vars.MAX_RETRY_ROUNDS || '2' }}
      REBALANCE_GUARD_ENABLED: ${{ vars.REBALANCE_GUARD_ENABLED || 'true' }}
      RUN_STATE_PATH: .runtime/live_state/rebalance_state.json
      RUN_LOCK_PATH: .runtime/live_state/rebalance.lock
      SAVE_DAILY_REPORT: ${{ vars.SAVE_DAILY_REPORT || 'true' }}
      REPORT_DIR: .runtime/live_reports
      ORDER_TIME_WAIT_ENABLED: ${{ vars.ORDER_TIME_WAIT_ENABLED || 'true' }}
      COMMISSION_FEE_RATE: ${{ vars.COMMISSION_FEE_RATE || '0.00015' }}
      EXISTING_POSITIONS_POLICY: ${{ vars.EXISTING_POSITIONS_POLICY || 'sell' }} # hold | sell

      KIWOOM_ORDER_ENDPOINT: ${{ vars.KIWOOM_ORDER_ENDPOINT || '/api/dostk/ordr' }}
      KIWOOM_ORDER_API_ID: ${{ vars.KIWOOM_ORDER_API_ID || 'kt10000' }}
      KIWOOM_ORDER_STATUS_ENDPOINT: ${{ vars.KIWOOM_ORDER_STATUS_ENDPOINT || '/api/dostk/acnt' }}
      KIWOOM_ORDER_STATUS_API_ID: ${{ vars.KIWOOM_ORDER_STATUS_API_ID || 'kt00007' }}
      KIWOOM_ORDER_CANCEL_ENDPOINT: ${{ vars.KIWOOM_ORDER_CANCEL_ENDPOINT || '/api/dostk/ordr' }}
      KIWOOM_ORDER_CANCEL_API_ID: ${{ vars.KIWOOM_ORDER_CANCEL_API_ID || 'kt10003' }}
      KIWOOM_QUOTE_ENDPOINT: ${{ vars.KIWOOM_QUOTE_ENDPOINT || '/api/dostk/mrkcond' }}
      KIWOOM_QUOTE_API_ID: ${{ vars.KIWOOM_QUOTE_API_ID || 'ka10004' }}
      KIWOOM_QUOTE_MARKET_TYPE: ${{ vars.KIWOOM_QUOTE_MARKET_TYPE || '0' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          clean: false

      - name: Validate self-hosted real runner
        run: |
          if [ "${{ runner.environment }}" != "self-hosted" ]; then
            echo "[ERROR] 이 워크플로는 self-hosted runner에서만 실행됩니다."
            exit 1
          fi

          if [ "${KIWOOM_MODE}" != "real" ]; then
            echo "[ERROR] 이 워크플로는 KIWOOM_MODE=real 고정입니다."
            exit 1
          fi

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Setup uv
        uses: astral-sh/setup-uv@v4

      - name: Prepare runtime directories
        run: |
          mkdir -p .runtime/live_state
          mkdir -p .runtime/live_reports

      - name: Install dependencies
        run: uv sync --frozen

      - name: Run live trading (real)
        run: |
          FORCE_FLAG=""
          DRY_FLAG=""
          SIGNAL_DATE=""

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            if [ "${{ github.event.inputs.force }}" = "true" ]; then
              FORCE_FLAG="--force"
            fi
            if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
              DRY_FLAG="--dry-run"
            fi
            SIGNAL_DATE="${{ github.event.inputs.signal_date }}"
          fi

          if [ -n "$SIGNAL_DATE" ]; then
            uv run run_live_trading.py --signal-date "$SIGNAL_DATE" $FORCE_FLAG $DRY_FLAG
          else
            uv run run_live_trading.py $FORCE_FLAG $DRY_FLAG
          fi

      - name: Upload real reports artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: live-real-reports-${{ github.run_id }}
          path: .runtime/live_reports
          if-no-files-found: ignore

      - name: Notify Telegram
        if: always() && env.TELEGRAM_BOT_TOKEN != '' && env.TELEGRAM_CHAT_ID != ''
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          JOB_STATUS: ${{ job.status }}
        run: |
          if [ "$JOB_STATUS" = "success" ]; then
            ICON="✅"
            MSG="실거래 워크플로 성공"
          else
            ICON="🚨"
            MSG="실거래 워크플로 실패"
          fi

          TEXT=$(printf "%s %s\nrepo=%s\nworkflow=%s\nrun_id=%s\nref=%s\nurl=%s/%s/actions/runs/%s" \
            "$ICON" \
            "$MSG" \
            "${{ github.repository }}" \
            "${{ github.workflow }}" \
            "${{ github.run_id }}" \
            "${{ github.ref_name }}" \
            "${{ github.server_url }}" \
            "${{ github.repository }}" \
            "${{ github.run_id }}")

          curl -sS -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            --data-urlencode "chat_id=${TELEGRAM_CHAT_ID}" \
            --data-urlencode "text=${TEXT}" \
            --data-urlencode "disable_web_page_preview=true"
